# Copy this file to .env and fill in real credentials.
# .env is gitignored — never commit real secrets.

MSUPPLY_USERNAME=User
MSUPPLY_PASSWORD=Pass

# Optional overrides — if set, these take precedence over config.json
# MSUPPLY_BASE_URL=https://release-demo-open.msupply.org
